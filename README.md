Encode mode: type once, watch it transcode live into ASCII, binary, hex, base64, morse, and braille all at once, each with its own copy button
Decode mode: pick a format, paste it back in, get plain text out
The strip across the top is a real punch-tape readout — 5-bit holes derived from whatever you're typing, teleprinter-style
